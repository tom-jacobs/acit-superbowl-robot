/* eslint-env jest */

// import router from '../../src/routes/router';

//
// Begin Tests
//

// Global variables for testing

test('placeholder while I figure out how to get real testing for endpoints', () => {
  expect(1).toBe(1);
});

/* describe('smoke test', () => {
  test('confirm we get a response from the router', async () => {
    expect.assertions(1);
    const req = {
      text: '',
      context: {},
    };
    const res = {};
    await expect(router.message(req, res)).resolves.toBeDefined();
  });
}); */
